export type OrderType = {
    orderId: string,
    email: string,
    address: string,
    postcode: string,
    orderItems: [],
    orderStatus: string,
    createdAt: string,
    updatedAt: string
}